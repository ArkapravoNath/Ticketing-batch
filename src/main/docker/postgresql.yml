version: '2'
services:
  ticketingbatch-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/TicketingBatch/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=TicketingBatch
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
